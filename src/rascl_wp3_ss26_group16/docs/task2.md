# Task 2 — Online motion planning

Structure for online pick-and-place:

- Receive cube centers from `/goal_poses` as `geometry_msgs/msg/Point`
- Plan from arbitrary cube positions inside the feasible region
- Place every cube at the known target at the maximum feasible radius

Implementation pending.
